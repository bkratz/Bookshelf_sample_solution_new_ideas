application:
  title: '@project.name@'
  version: '@project.version@'

bookshelf:
  title: 'Booksheld API'
  version: 'Version 1'
  capacity: 1000
  description: 'This bookshelf has a capacity of: '
  license:
    name: 'MIT License'
    url: 'https://opensource.org/licenses/MIT'

server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
  h2:
    console:
      enabled: true
